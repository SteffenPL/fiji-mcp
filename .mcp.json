{
  "mcpServers": {
    "fiji-mcp": {
      "command": "uv",
      "args": ["run", "--directory", ".", "fiji-mcp", "--save-trace", "traces"],
      "env": {
        "FIJI_MCP_PORT": "8765"
      }
    }
  }
}
