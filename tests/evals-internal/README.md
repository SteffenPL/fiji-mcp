# evals-internal

Reference algorithms for the eval harness. These live outside `evals/` so that subagents running eval tasks cannot see the ground truth approach and copy it.

Only the orchestrator accesses this directory.
